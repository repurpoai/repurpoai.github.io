1. Setup instructions

Fill these environment variables:
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
TOGETHER_API_KEY
TOGETHER_MODEL
TOGETHER_IMAGE_MODEL
NEXT_PUBLIC_PRO_UPGRADE_URL

What NEXT_PUBLIC_PRO_UPGRADE_URL does:
All paid upgrade buttons point to this URL. You can use one checkout link for Plus / Pro, a billing page, or any custom upgrade destination. If you leave it empty, upgrade buttons fall back to /pricing.

Supabase setup for a fresh project:
Create a Supabase project.
Open SQL Editor.
Paste the full contents of supabase/schema.sql.
Run it.

If you already created the database from the older Version 1 schema, run supabase/v2_upgrade.sql after that.

In Project Settings → API, copy:
Project URL
Publishable key

In Authentication → Providers → Email, keep email/password enabled.
Disable Confirm Email if you want signup to land in the dashboard immediately.

How plans work in this build:
Free users get:
tier = 'free'
monthly_generation_limit = 5

Plus users get:
tier = 'plus'
monthly_generation_limit = null
billing_status = 'active'

Pro users get:
tier = 'pro'
monthly_generation_limit = null
billing_status = 'active'

Manual upgrade SQL for testing paid plans:
update public.profiles
set
  tier = 'plus',
  monthly_generation_limit = null,
  billing_status = 'active'
where email = 'user@example.com';

Or for Pro:
update public.profiles
set
  tier = 'pro',
  monthly_generation_limit = null,
  billing_status = 'active'
where email = 'user@example.com';

Together AI setup:
Create an API key in Together AI and place it in TOGETHER_API_KEY.
Recommended text model default: Qwen/Qwen3.5-9B
Recommended image model default: black-forest-labs/FLUX.1-schnell-Free
The FLUX free endpoint is useful for testing, but it has tighter rate limits than paid endpoints.

2. Run instructions

Install dependencies:
npm install

Create .env.local and paste:
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key
TOGETHER_API_KEY=your-together-ai-api-key
TOGETHER_MODEL=Qwen/Qwen3.5-9B
TOGETHER_IMAGE_MODEL=black-forest-labs/FLUX.1-schnell-Free
NEXT_PUBLIC_PRO_UPGRADE_URL=https://your-checkout-or-billing-url.com

Start local dev:
npm run dev

Open:
http://localhost:3000

Vercel deploy:
Push the project to GitHub
Import the repo into Vercel
Add the same environment variables in Vercel Project Settings
Deploy

Production notes:
This app already includes:
plan-aware UI locks
server-enforced monthly usage caps
DB tier tracking
private history saving
multi-platform generation
YouTube transcript mode
in-app image generation on paid tiers
